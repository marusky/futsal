<div>
  <h1 class="font-bold text-4xl">Players#new</h1>
  <%= form_with model: @player do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name, autocomplete: 'name' %>
    <%= f.submit %>
  <% end %>
</div>
